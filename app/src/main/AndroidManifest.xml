<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ren.face">

    <uses-feature android:name="android.hardware.camera" /> <!-- 相应的权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".acitivity.Delete"></activity>
        <activity
            android:name=".acitivity.RegisterFace"
            android:label="添加人脸信息" />
        <activity
            android:name=".acitivity.QueryActivity"
            android:label="查询" />
        <activity
            android:name=".acitivity.Menu"
            android:label="菜单" />
        <activity android:name=".acitivity.Register" />
        <activity
            android:name=".MainActivity"
            android:label="Face">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>